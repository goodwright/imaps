import React from 'react';
import PropTypes from 'prop-types';

const App = () => {
    return (
        <div>
            iMaps
        </div>
    );
};

App.propTypes = {
    
};

export default App;