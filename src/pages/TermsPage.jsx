import React from "react";
import PropTypes from "prop-types";
import { Link } from "react-router-dom";
import Base from "./Base";

const TermsPage = props => {
    return (
        <Base className="terms-page">
            Terms and Conditions
        </Base>
    );
};

TermsPage.propTypes = {
    
};

export default TermsPage;